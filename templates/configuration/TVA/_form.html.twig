{{ form_start(form)}}
    {{ form_row(form.Libelle)}}
    {{ form_row(form.Taux)}}
    {{ form_row(form.DateDebut)}}
    {{ form_row(form.DateFin)}}
    <button type="submit" class="btn btn-primary">{{ button_label|default('Créer la TVA')}}</button>
{{ form_end(form)}}